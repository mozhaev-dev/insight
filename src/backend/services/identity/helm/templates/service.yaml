apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-identity-resolution.fullname" . }}
  labels:
    {{- include "insight-identity-resolution.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-identity-resolution.selectorLabels" . | nindent 4 }}
