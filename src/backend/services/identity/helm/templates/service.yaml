apiVersion: v1
kind: Service
metadata:
  name: {{ include "insight-identity.fullname" . }}
  labels:
    {{- include "insight-identity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insight-identity.selectorLabels" . | nindent 4 }}
